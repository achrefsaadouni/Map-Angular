import { FiltreResourcePipe } from './filtre-resource.pipe';

describe('FiltreResourcePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreResourcePipe();
    expect(pipe).toBeTruthy();
  });
});
